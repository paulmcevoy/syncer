# Core Configuration
# REQUIRED: You must set these values before using the system
SOURCE_DIR=/path/to/source                # Directory containing files to sync
DEST_DIR=/path/to/destination             # Directory to sync files to
MOUNT_POINT=/path/to/mount                # Mount point for external drive
LOG_FILE=/path/to/sync.log                # Path to log file

# SMS Configuration (REMOVED)

# Telegram Configuration (Optional - Required if enabled during install)
# BOT_TOKEN=your_telegram_bot_token         # Your Telegram Bot Token from BotFather
# CHAT_ID=your_telegram_chat_id             # The chat ID to send messages to (user or group)

# Tidal Configuration (Optional)
# Uncomment and fill these if you installed the Tidal module
#TIDAL_QUALITY=LOSSLESS                   # Options: LOSSLESS, HIGH, LOW
#TIDAL_DEFAULT_URL=                       # Default Tidal URL to download from (optional)
#TIDAL_DOWNLOAD_DIR=/path/to/music        # Directory where Tidal downloads files to (if SOURCE_DIR is not set)
# Note: If SOURCE_DIR is set, it will be used as the download directory for Tidal